package com.fittribe.api.entity;

public enum AuthProvider {
    PHONE,   // Firebase phone OTP
    EMAIL,   // Firebase email/password
    GOOGLE,  // Firebase Google OAuth
    APPLE    // Firebase Apple OAuth
}
